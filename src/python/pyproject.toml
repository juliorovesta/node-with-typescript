[tool.poetry]
name = "python-app"
version = "0.1.0"
description = ""
authors = ["Julio Cesar"]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
